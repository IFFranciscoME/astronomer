
{
  "tables": [
    {
      "name": "Agents",
      "columns": [
        {
          "name": "agent_id",
          "type": "SERIAL",
          "constraints": ["PRIMARY KEY", "NOT NULL"]
        },
        {
          "name": "agent_major_type",
          "type": "VARCHAR(100)",
          "constraints": ["NOT NULL"]
        },
        {
          "name": "agent_minor_type",
          "type": "VARCHAR(100)",
          "constraints": ["NOT NULL"]
        },
        {
          "name": "is_active",
          "type": "BOOLEAN",
          "constraints": ["DEFAULT TRUE"]
        }
      ]
    }
  ]
}

